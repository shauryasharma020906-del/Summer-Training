#include <iostream>
using namespace std;

int main() {
    int a, b, ans;
    cout << "Input two numbers separated by a space: ";
    cin >> a >> b;
    ans = a + b;
    cout << "Total = " << ans << "\n";
    return 0;
}